{
  "name": "vitamood-functions",
  "description": "VitaMood Cloud Functions — OpenAI proxy, usage metering, safety backstop.",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "bun run build && firebase emulators:start --only functions",
    "shell": "bun run build && firebase functions:shell",
    "start": "bun run shell",
    "deploy": "bun run build && firebase deploy --only functions",
    "logs": "firebase functions:log",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "openai": "^4.68.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.6.0"
  }
}
